# 더브릿지교회 웹앱 — 푸시 알림 도입 계획

작성일 2026-08-20 · 대상: 하노이 더브릿지교회 웹앱

---

## 0. 한 장 요약

| 항목 | 내용 |
|---|---|
| **지금** | 앱 안에 **알림함**은 이미 완성 (헤더 🔔 → 목록). 자동 알림 9종도 서버에서 정상 작동 중 |
| **목표** | 앱을 열지 않아도 **휴대폰 화면에 알림이 뜨게** 하는 것 |
| **추가 비용** | **0원** (Supabase·Vercel 무료 범위 안, 별도 서비스 가입 불필요) |
| **총 작업 기간** | 실제 작업 3~4일 + 테스트 1~2주 (성도님 대상 시범운영 포함) |
| **가장 큰 걸림돌** | 아이폰. **홈 화면에 앱을 추가한 사람만** 푸시를 받을 수 있음 |
| **핵심 원칙** | 알림 권한을 **절대 자동으로 묻지 않는다.** 성도님이 스위치를 켤 때만 묻는다 |

---

## 1. 지금 무엇이 되어 있나

이미 만들어 둔 것 (추가 작업 없이 그대로 재사용합니다):

- **알림 저장소** — `notifications` 표. 누구에게, 어떤 종류, 제목, 내용, 읽음 여부가 다 들어 있습니다.
- **알림함 화면** — 헤더의 🔔 를 누르면 목록이 나오고, 누르면 그 화면으로 이동합니다.
- **자동 알림 9종** — 서버가 정해진 시각에 알아서 넣어줍니다.

| 알림 | 시각(베트남) |
|---|---|
| 식사 미응답 1차 / 2차 | 금 20:00 / 토 12:00 |
| 새 주보 | 금·토 20:00 |
| 생일 축하 | 매일 08:00 |
| 출석 리마인더 1·2·3차 | 일 20:00 / 월 12:00 / 월 20:00 |
| 오래된 알림 정리 | 매일 04:00 |

- **직접 보내기** — 관리 > 알림 화면에서 그룹·개인을 골라 보낼 수 있습니다.
- **서비스워커** (`public/sw.js`) — 이미 등록되어 있습니다. **푸시를 받아 화면에 띄우는 것도 이 파일이 하는 일**이라, 새로 만들 필요 없이 여기에 기능만 덧붙이면 됩니다.

> 👉 **이미 8할은 되어 있습니다.** 남은 일은 "저장된 알림을 휴대폰 밖으로 내보내는 통로"를 다는 것뿐입니다.

---

## 2. 알림함과 푸시는 무엇이 다른가

|  | 알림함 (지금) | 푸시 알림 (앞으로) |
|---|---|---|
| 언제 보이나 | 앱을 **열어야** 보임 | 앱을 안 열어도 **휴대폰이 울림** |
| 권한 | 필요 없음 | 성도님이 **허용**해야 함 |
| 잘못 보내면 | 조용히 목록에만 쌓임 | **성도님 폰이 울림** (되돌릴 수 없음) |
| 기기 제약 | 없음 (아이폰·안드로이드·PC 동일) | **아이폰은 홈 화면 추가 필수** |

즉 푸시는 "더 좋은 알림"이 아니라 **더 센 알림**입니다. 그래서 도입 자체보다 **어떤 알림을 푸시로 내보낼지 고르는 일**이 더 중요합니다. (→ 4단계)

---

## 3. 기기별 현실 — 미리 알고 계셔야 할 제약

### 안드로이드 (성도님 대부분)
- 브라우저(크롬)에서 바로 푸시를 받을 수 있습니다. 홈 화면 추가 안 해도 됩니다.
- 가장 문제가 적습니다.

### 아이폰 — ⚠️ 여기가 관건
1. **iOS 16.4 이상**이어야 합니다. (2023년 3월 이후 업데이트. 대부분 해당됩니다)
2. **반드시 홈 화면에 앱을 추가한 상태**여야 합니다.
   사파리에서 그냥 보는 상태로는 **푸시가 아예 불가능**합니다. 애플 정책이라 우회할 방법이 없습니다.
3. 권한 요청은 **성도님이 버튼을 누른 직후에만** 띄울 수 있습니다. 자동으로 띄우면 무시됩니다.
4. **한 번 "거부"를 누르면 되돌릴 방법이 사실상 없습니다.**
   설정 앱에서 다시 켜는 메뉴가 없고, **홈 화면 앱을 지웠다가 다시 추가**해야 합니다.

> 이 4번이 이번 계획에서 가장 조심해야 할 부분입니다.
> 어르신 성도님이 무슨 창인지 모르고 "허용 안 함"을 누르시면, 그분은 앱을 지웠다 다시 까시기 전까지 영영 푸시를 못 받습니다.
> **그래서 "권한을 묻는 창을 언제 띄우느냐"를 제일 보수적으로 설계했습니다.**

### PC (윈도우·맥)
- 크롬·엣지에서 잘 됩니다. 사무용으로 관리자분들께 유용합니다.

---

## 4. 어떤 알림을 푸시로 내보낼까 — 권장안

**전부 다 내보내면 안 됩니다.** 폰이 자주 울리면 성도님들이 알림을 꺼버리고, 그러면 정작 중요한 알림도 못 받습니다.

| 알림 종류 | 푸시 | 이유 |
|---|---|---|
| 식사 미응답 1차·2차 | ✅ **보냄** | 마감이 있어 놓치면 실제로 곤란함 |
| 새 주보 | ✅ **보냄** | 주 1회, 전 성도가 기다리는 내용 |
| 출석 리마인더 | ✅ **보냄** | 받는 사람이 리더 몇 분뿐 |
| 관리자 직접 알림 | ✅ **보냄** | 급한 공지용. 단 발송 전 확인창 유지 |
| 생일 축하 | 🔸 **선택** | 본인에게만 가니 부담 적음. 켜도 무방 |
| 새 공지 | 🔸 **선택** | 공지가 잦아지면 시끄러워짐 |
| 댓글·좋아요 | ❌ **안 보냄** | 가장 자주 발생. 알림함에서만 확인 |

> 이 표는 제안이며, 목사님·관리자분들과 상의해 조정하시면 됩니다.
> 나중에 관리 화면에서 항목별로 켜고 끌 수 있게 만들 수도 있습니다 (5단계).

---

## 5. 단계별 작업 계획 (0단계 ~ 5단계)

### 0단계 — 준비 (내가 함 · 반나절)

무엇을 | 설명
---|---
**VAPID 열쇠 만들기** | 푸시를 보낼 때 "이게 진짜 더브릿지 서버가 보낸 것"임을 증명하는 열쇠 한 쌍입니다. 무료이고 한 번 만들면 끝입니다.
**열쇠 보관** | 공개 열쇠는 앱에, 비밀 열쇠는 Supabase 서버에 각각 넣습니다.
**구독 저장 표 만들기** | `push_subscriptions` — 누가 어느 기기에서 알림을 받기로 했는지 저장합니다. (한 사람이 폰·PC 여러 대 가능)

> **사장님이 하실 일:** 없음. 다만 Vercel 환경변수 한 줄을 제가 알려드리면 넣어주셔야 할 수 있습니다.

---

### 1단계 — "휴대폰 알림 받기" 스위치 (내가 함 · 1일)

- **내정보 탭**에 스위치를 하나 넣습니다. 이름은 `📱 휴대폰 알림 받기`.
- 이 스위치를 **누를 때만** 브라우저 권한 창이 뜹니다. 앱을 켜자마자 뜨는 일은 절대 없습니다.
- 스위치 아래에 상태를 한글로 설명합니다:

| 상태 | 화면에 보이는 글 |
|---|---|
| 아직 안 켬 | "켜시면 식사 신청·주보 알림을 휴대폰으로 받으실 수 있습니다" |
| 켜짐 | "✅ 이 기기로 알림을 받고 있습니다" |
| 거부됨 | "알림이 차단되어 있습니다. 홈 화면의 앱을 지우고 다시 추가하시면 다시 여쭤봅니다" |
| 아이폰인데 홈 화면 추가 안 함 | "아이폰은 먼저 홈 화면에 앱을 추가하셔야 합니다 → [추가 방법 보기]" |

> **왜 스위치로 하나요?** 3-4에서 말씀드린 "한 번 거부하면 끝" 문제 때문입니다.
> 성도님이 **무슨 창인지 알고** 누르게 만들면 거부율이 크게 떨어집니다.

---

### 2단계 — 보내는 쪽 만들기 (내가 함 · 1일)

- Supabase **Edge Function** `send-push` 를 만듭니다.
  `notifications` 표에 새 줄이 들어오면 → 그 사람의 등록된 기기들로 푸시를 쏩니다.
- **기존 자동 알림 함수는 손대지 않습니다.** 지금처럼 알림을 저장만 하면, 새 함수가 알아서 뒤따라 보냅니다.
  → **혹시 푸시 쪽이 고장 나도 기존 알림함은 멀쩡합니다.**
- 죽은 기기(앱을 지운 폰 등)는 자동으로 목록에서 지웁니다.

---

### 3단계 — 받는 쪽 만들기 (내가 함 · 반나절)

- `public/sw.js`에 두 가지를 덧붙입니다.
  - `push` — 푸시가 오면 **교회 로고와 함께** 알림을 띄웁니다.
  - `notificationclick` — 알림을 누르면 **그 알림에 맞는 화면**으로 앱을 엽니다. (식사 알림 → 신청>주일식사 등, 이미 만들어 둔 규칙 재사용)

---

### 4단계 — 시험 운영 (같이 함 · 1~2주)

1. **관리자 3~5명만** 먼저 스위치를 켜고 씁니다. (안드로이드·아이폰 섞어서)
2. 일주일간 실제 자동 알림(식사·주보)을 받아보며 확인:
   - 알림이 제때 오는가
   - 눌렀을 때 맞는 화면이 열리는가
   - 아이폰에서 잠금화면에도 뜨는가
   - 너무 자주 울리지 않는가
3. 문제가 없으면 4단계 표대로 내보낼 알림 종류를 확정합니다.

> **사장님이 하실 일:** 관리자 몇 분께 부탁드려 스위치를 켜게 하고, 일주일 뒤 소감을 모아주시면 됩니다.

---

### 5단계 — 전체 공개 + 안내 (같이 함 · 1일)

- 주보 또는 단톡방으로 안내합니다. 안내문에 **꼭 들어가야 할 3줄**:
  1. 내정보 > `📱 휴대폰 알림 받기`를 켜주세요
  2. **아이폰은 먼저 홈 화면에 앱을 추가**해 주세요 (방법 그림 포함)
  3. 권한 창이 뜨면 **"허용"**을 눌러주세요 (거부하면 다시 켜기 번거롭습니다)
- 앱 안에도 한 번만 보이는 짧은 안내 카드를 띄웁니다. (버튼은 "알림 켜기 / 나중에" 두 개뿐)

**나중에 여유 있을 때 (선택):**
- 관리 화면에서 알림 종류별 푸시 on/off
- 성도님 개인이 "식사 알림만 받기" 같은 선택
- 조용한 시간대(밤 10시~아침 7시) 자동 보류

---

## 6. 일정표

| 단계 | 누가 | 걸리는 시간 |
|---|---|---|
| 0. 준비 (열쇠·표) | 저 | 반나절 |
| 1. 스위치 화면 | 저 | 1일 |
| 2. 보내는 함수 | 저 | 1일 |
| 3. 받는 서비스워커 | 저 | 반나절 |
| **여기까지 개발** | | **3~4일** |
| 4. 관리자 시험 운영 | 같이 | 1~2주 |
| 5. 전체 공개 | 같이 | 1일 |

---

## 7. 비용

| 항목 | 비용 |
|---|---|
| VAPID 열쇠 | 0원 (직접 생성) |
| Supabase Edge Function | 무료 범위 (월 50만 회, 우리는 월 수천 회 수준) |
| 푸시 전송 (구글·애플 서버) | 0원 (웹 표준, 과금 없음) |
| Vercel | 기존 그대로 |
| **합계** | **0원** |

> 파이어베이스(FCM) 같은 외부 서비스에 가입할 필요가 **없습니다.** 웹 표준 푸시를 쓰기 때문입니다.

---

## 8. 위험과 대비책

| 위험 | 얼마나 | 대비책 |
|---|---|---|
| 어르신이 권한 창에서 "거부"를 누름 | **높음** | 스위치 방식 + 사전 안내 + 스위치 아래에 복구 방법 안내 |
| 아이폰인데 홈 화면 추가를 안 함 | **높음** | 아이폰이면 스위치 대신 "홈 화면 추가 방법"을 먼저 보여줌 |
| 알림이 너무 자주 울려 성도님이 꺼버림 | 중간 | 댓글·좋아요는 푸시 제외 (4단계 표) |
| 관리자가 실수로 전체 발송 | 중간 | 지금도 있는 확인창 유지 + "폰이 울립니다" 문구 추가 |
| 푸시 기능이 고장남 | 낮음 | 알림함과 **완전히 분리**되어 있어 알림함은 계속 정상 작동 |
| 아이폰 iOS 16.4 미만 | 낮음 | 스위치를 숨기고 "iOS 업데이트가 필요합니다" 안내 |

---

## 9. 결정해 주셔야 할 것

시작하기 전에 아래 3가지만 정해주시면 바로 0단계에 들어갈 수 있습니다.

1. **푸시로 내보낼 알림 종류** — 4번 표 그대로 갈지, 생일·공지도 넣을지
2. **시험 운영에 참여하실 분** — 관리자 3~5명 (아이폰 사용자 최소 1명 필수)
3. **언제 시작할지** — 주보·식사 신청이 한가한 주간이 좋습니다

---

## 부록 — 아이폰 홈 화면 추가 방법 (성도님 안내용)

1. **사파리**로 교회 앱 주소를 엽니다. (크롬 말고 사파리여야 합니다)
2. 화면 아래 가운데 **공유 버튼** (□에 ↑ 표시)을 누릅니다.
3. 목록을 아래로 내려 **"홈 화면에 추가"**를 누릅니다.
4. 오른쪽 위 **"추가"**를 누릅니다.
5. 홈 화면에 생긴 **교회 로고 아이콘**으로 앱을 엽니다.
6. 내정보 > `📱 휴대폰 알림 받기`를 켜고, 창이 뜨면 **"허용"**을 누릅니다.
